{
    "name": "olha-isso",
    "version": "1.0.0",
    "license": "MIT",
    "private": true,
    "dependencies": {
        "serverless": "^1.36.3"
    },
    "scripts": {
        "deploy": "source ./.env && serverless deploy -r $AWS_DEFAULT_REGION -s ${STAGE:-prod} -v"
    }
}